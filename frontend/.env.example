# Backend API (Django REST Framework)
VITE_API_BASE_URL=http://localhost:8000/api

# Google Maps (loaded only when key is present; app falls back to the
# custom static-preview MapView otherwise — see src/components/maps/MapView.jsx)
VITE_GOOGLE_MAPS_API_KEY=

# Future AI assistant backend
VITE_AI_API_BASE_URL=http://localhost:8000/api/ai
